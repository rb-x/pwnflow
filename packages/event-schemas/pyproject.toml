[project]
name = "pwnflow-event-schemas"
version = "0.1.0"
description = "Shared event schema definitions for Pwnflow services"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
